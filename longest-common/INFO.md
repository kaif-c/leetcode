# Longest Common Prefix

## Difficulty: Easy

**Description**:

Find longest common prefix prefix in array

**Constraints**:

- 1 <= strs.length <= 200
- 0 <= strs[i].length <= 200
- strs[i] consists of only lowercase English letters if it is non-empty.
